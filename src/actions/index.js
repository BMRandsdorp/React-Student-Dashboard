export const nameCheckChange = (values) => {
  return {
    type: `CHECKCHANGE`,
    payload: values,
  };
};

/*export const nameUnchecked = (value) => {
  return {
    type: `UNCHECKED`,
    payload: value,
  };
};*/
